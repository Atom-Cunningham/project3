#include <stdlib.h>

    /**With each request, increment brk by size
     * return old brk
     * 
     */
int *my_firstfit_malloc(int size){
    
     
    
    return 0;
}

/**take a pointer to the start of the region
 * which should be LL bookeeping data
 * 
 */
int my_free(void *ptr){
    return 0;
}